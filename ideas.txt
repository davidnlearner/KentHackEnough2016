DJ feedback app
MMO Scrabble game
Phishing detection browser extension
 - host with AWS on domain.com-purchased domain



function url_domain(data) {
  var    a      = document.createElement('a');
         a.href = data;
  return a.hostname;
}
